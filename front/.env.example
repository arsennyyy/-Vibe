# Пусто = запросы /api через Vite proxy в dev
# VITE_API_URL=http://localhost:5000

# Google Sign-In — тот же Client ID, что в MyMvcBackend/appsettings.json → Google:ClientId
# Можно оставить пустым: фронт подтянет ID с GET /api/auth/google-config
VITE_GOOGLE_CLIENT_ID=
